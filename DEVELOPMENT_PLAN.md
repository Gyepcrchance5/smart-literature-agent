# Agent 开发与作品交付执行计划

更新：2026-09-13。状态：已确定执行顺序，待进入 A1。
基线提交：`9205d98`。后续“继续开发”默认按本文件执行。

## 1. 目标与完成定义

产品目标：对话驱动的科研任务工作台。用户提出文献分析任务，系统执行计划、
获取证据、比较方法、形成有出处的建议，并展示状态、失败原因、耗时和 token。

求职目标：用可复现代码、真实运行、故障恢复和评测对照，证明 Agent 开发岗位所需的
Planning、Tool Use、Memory、Reflection、框架编排和可靠性能力。

代表任务：

> 比较两篇论文，结合一个故障诊断项目的已授权背景，指出可迁移方法、限制与待验证假设。

最终交付必须同时具备：

- 独立运行的 Python 后端和轻量 Web 工作台。
- 自研 Runtime 与 LangGraph 后端共享工具、证据和评测契约。
- 跨进程检查点恢复、受限重试、明确的任务结果和成本记录。
- 原文证据定位、结构化方法比较、可解释的缺证据回答。
- 固定任务集上的真实评测与人工审核；离线测试、录制回放、真实调用分别统计。
- 一次有成本记录的单/多 Agent 对照实验，结论可以是不采用协作作为默认。
- 公开样例、运行说明、架构图、三分钟录屏、技术取舍记录。

## 2. 已完成基线与明确缺口

已完成：文献流水线、自研 Runtime、计划依赖/工具门、任务完成判定、失败记忆隔离、
无样本指标修正、本地项目有限扫描。29 个离线测试通过，无 LLM 报告流水线已验证落盘。

仍缺：真实模型成功基线（上次请求 401）、LangGraph、跨进程任务恢复、原文论断核验、
检索基准、完整多轮指代、Web 展示、协作实验。上述能力不能提前写成已完成成果。

St_distll_prun 是本地真实验收项目：只读已允许的代码、说明与配置。
扫描代码类清单不等于确认正式启用模块；配置声明不等于核验 run 结果。
科研项目的当前说明和正式配置优先于文献工具中旧的科研背景。

本轮已授权过的真实测试是：本地知识蒸馏问答，以及 2107.04689 / 2109.14873 比较，
每条最多 4,800 输出 token。授权继续有效，不重复询问同一范围。
批量评测执行前应列明数据、服务、次数和预算；未公开科研内容不因本计划而获得外发授权。

## 3. 技术边界与建议结构

以下是目标结构，新增路径均是计划项，当前不存在不代表遗漏：

```text
Web / CLI
  └─ Task service：任务 ID、状态、取消/恢复、事件流
      ├─ Native adapter：现有自研 Runtime
      └─ LangGraph adapter：状态图、条件分支、持久检查点
           └─ Shared tools / evidence / outcome / budget contracts
                ├─ 检索、读取、比较、项目有限扫描
                ├─ 原文证据与结构化产物
                └─ 本地评测、trace、session memory
```

- 保留 `src/run.py` CLI；通过后端选择接入 LangGraph，不复制工具实现。
- LangGraph 本地持久化优先使用 SQLite；安装时锁定实际兼容版本并记录依赖。
- 短期图状态与长期已验证记忆分开；检查点不自动授予答案“已验证”资格。
- 展示层采用 FastAPI + 轻量 HTML/CSS/JavaScript，先用 SSE 展示执行事件。
  首版不引入复杂前端状态库；业务判断留在 Python 后端。
- 原始 trace 与个人库留本地；公开演示样例使用可公开或合成数据，并标明来源。
- 不启动科研训练、不修改数据划分、不替换冻结资产；不以本项目训练自己的大模型。

参考：[LangGraph overview](https://docs.langchain.com/oss/python/langgraph/overview)、
[Persistence](https://docs.langchain.com/oss/python/langgraph/persistence)。实现时复核对应安装版本文档。

## 4. 里程碑与任务清单

状态：`TODO` 未开始；`DOING` 正在执行；`BLOCKED` 外部条件阻塞；`DONE` 已验收。
预计文件只是改动定位，可按实现调整；验收条件不能未经说明自行删除。

### A. 凭证、任务集与可复现基线

依赖：现有基线。优先处理 A1；遇到凭证阻塞，继续 A2/A3 和 B 的离线实现。

| ID | 状态 | 工作 | 验收与交付 |
|---|---|---|---|
| A1 | DONE | 核对 provider/key/base_url/model 来源及优先级，修复配置误配和日志脱敏 | 显式配置覆盖规则有测试；401 清晰归类；不输出 key、不跨服务试用其他 key |
| A2 | TODO | 固定公开/合成样例、任务 schema、模型/提示词/数据版本、计数方式 | 干净检出可运行离线套件；个人库不是测试前提 |
| A3 | TODO | 编制 30 条开发任务与 10 条独立验收任务，建立人工 rubric | 覆盖问答、比较、审查、多轮、缺材料、失败恢复；每条明确期望行为与证据边界 |
| A4 | TODO | 有效凭证就绪后执行已授权的两条真实测试 | 记录工具轨迹、答案、token、耗时和人工结论；任一失败保留原因，不改写成通过 |

定位：`src/utils.py`、`src/summarizer.py`、`scripts/acceptance_probe.py`、
`config/`、计划新增 `tests/fixtures/` 与评测任务文件。
产物：基线报告、复现命令、任务集与配置来源说明。

### B. LangGraph 与可恢复任务执行

依赖：A2；真实验收依赖 A4。自研后端继续可用。

| ID | 状态 | 工作 | 验收与交付 |
|---|---|---|---|
| B1 | TODO | 定义共享 TaskState、Evidence、ToolResult、TaskOutcome、Budget 和事件 schema | 两后端统一字段；响应完成与任务成功分离；CLI 可选择后端 |
| B2 | TODO | 实现计划→工具→证据检查→回答→收尾的 LangGraph 状态图 | 相同输入通过同一离线契约测试；保留工具白名单、依赖检查和记忆门 |
| B3 | TODO | SQLite 检查点、稳定 task/session ID、恢复入口 | 独立进程终止后可恢复；完成步骤有幂等键/结果缓存；测试正常步骤之间及工具返回附近的中断 |
| B4 | TODO | 总期限、有限重试、退避、取消、输出/调用预算 | 超时与取消可终止；恢复不重置预算；收束和修复调用也计入预算 |
| B5 | TODO | 用同一真实任务对照 Native 与 LangGraph | 记录完成率、失败类型、token、延迟；不假定框架必然提高回答质量 |

恢复设计必须说明：哪些操作可以重复，哪些必须缓存或幂等；不能泛称“框架保证恰好执行一次”。
取消状态不得伪装成完成；无法终止的外部请求应明确记录实际边界。
产物：状态图、恢复演示、两后端对照、架构决策记录。

### C. 证据检索、论断与多轮记忆

依赖：A3、B1；可在 B 的真实测试等待期间推进离线部分。

| ID | 状态 | 工作 | 验收与交付 |
|---|---|---|---|
| C1 | TODO | 建立论文 section/段落切片、稳定 evidence ID、来源与读取策略 | 引用可跳回确切原文片段；预览/元数据边界不丢失 |
| C2 | TODO | 标注检索相关性，评测现有关键词基线，再实现一个混合检索候选 | 同集报告 Recall@K/MRR、样本数与延迟；按结果决定是否增加 reranker |
| C3 | TODO | 输出 claim→evidence 映射，区分事实、推断和待验证假设 | “有效 ID + 虚构 99.99%”故障案例不能作为已验证事实通过；拒答不能冒充任务成功 |
| C4 | TODO | 支持“第二篇/它/上次方案”等指代，记忆带来源与版本 | 跨 session 不串数据；模糊指代澄清；失败/过期/来源变化的结论不直接复用 |
| C5 | TODO | 本地项目背景与正式配置接入任务契约 | 正式启用/禁用模块优先；不把扫描到的所有类当成当前方案；私人背景保持授权边界 |

论断检查不能仅凭关键词或另一模型的打分就标成事实保证；最终评测保留人工原文核对。
产物：证据样例、检索对照、论断支持率、记忆回归报告。

### D. 可演示的科研任务工作台

依赖：B1/B2。可先用合成事件开发界面；证据功能接 C1/C3，恢复功能接 B3/B4。

| ID | 状态 | 工作 | 验收与交付 |
|---|---|---|---|
| D1 | TODO | Python task service：创建、查询、取消、恢复与 SSE 事件 | API/CLI 共用后端；任务 ID 稳定；客户端重连不创建重复任务 |
| D2 | TODO | 任务区、执行区、证据结果区三栏界面 | 展示工具状态、缺证据原因、出处、耗时、token；N/A 与零正确区分 |
| D3 | TODO | 真实执行与录制回放模式 | 全程明显标记 live/replay；回放无外部调用；界面断线后可查到原任务 |
| D4 | TODO | 对独立应用做端到端验收 | 用户无需借助开发助手，可自行启动应用、提交任务、查证据、恢复任务 |

默认仅本机访问；任务 API 不暴露任意 shell 或无限制文件浏览；连接科研项目使用显式配置范围。
执行区展示可观察的计划、工具事件和简短决策说明，不声称展示模型隐藏思维链。
产物：可运行 UI、操作说明、三段演示草稿。

### E. 有证据的多 Agent 协作实验

依赖：B/C 通过。以单 Agent 为基线，协作是受控实验，不是预定胜者。

| ID | 状态 | 工作 | 验收与交付 |
|---|---|---|---|
| E1 | TODO | 定义检索、证据审查、综合三个角色的数据协议与工具权限 | 有明确输入/输出、证据 ID、失败状态；共享状态由调度器管理，避免相互覆盖 |
| E2 | TODO | 实现受限协作与独立论文分析并行 | 受总预算/并发限制；超时、失败、取消能传播；没有无限互相讨论 |
| E3 | TODO | 单/多 Agent 同任务与同总预算对照 | 比较支持率、任务成功、延迟和 token；报告额外成本及不利结果 |

只有测得明确收益才切换默认；否则保留实验后端与技术记录，仍算完成该阶段。
本阶段指产品内的协作实现，不要求开发助手默认派出子代理。

### F. 发布与秋招作品交付

依赖：A–D 验收，E 实验有结论。公开版本只包含审核过的材料。

| ID | 状态 | 工作 | 验收与交付 |
|---|---|---|---|
| F1 | TODO | 冻结版本，执行 10 条独立验收任务并人工复核 | 原始分母和失败类别齐全；发现失败应修复并记录复测，不能删案例美化指标 |
| F2 | TODO | README 首屏、架构图、选型记录、评测报告、启动说明 | 干净环境能运行离线 demo；真实运行所需配置明确；不含凭证和私人科研产物 |
| F3 | TODO | 三分钟录屏：正常任务、失败恢复、修复/评测对照 | 画面和数据来自冻结版本；回放标识明确；成果数字可回溯 |
| F4 | TODO | 整理面试讲解与技术文章草稿 | 能解释状态机、记忆边界、工具策略、RAG、框架取舍、协作收益及成本；不写未实现能力 |

## 5. 统一验收方法

本计划不预填成功率、性能提升或费用结论。发布目标：确定性契约与故障回归全部通过；
独立真实任务目标至少 8/10 完成且有人工认可的证据支持。该目标是门槛，不是已有成绩，
10 条样本也不能代表生产可靠性。未达到时保留 beta 标记和失败清单。

- **任务成功**：必需步骤完成、无策略违规、预算合规、结论符合任务 rubric 且证据足够。
- **可追溯性**：引用存在并能定位来源；与“来源支持具体论断”的人工支持率分开报告。
- **检索**：固定 K、固定标注集，报告 Recall@K/MRR 与样本数。
- **成本**：输入/输出 token、工具调用数、总耗时；已知价格才计算费用，未知费用写 N/A。
- **性能**：记录原始耗时，小样本不包装稳定 p95；比较使用相同模型/数据/任务/预算。
- **恢复**：检查点跨进程有效、任务 ID 不变、预算不重置、副作用可控。
- **样本隔离**：合成响应、真实模型、录制回放分别标记，不能混算任务成功率。

日常检查：针对本次变更的回归测试、必要 CLI/UI 冒烟、`git diff --check`。
只有相关变更或失败才扩大测试；文档修改不重跑模型和整个业务流水线。

## 6. 后续执行协议

1. 先读本文件的进度表、`git status` 和最近提交；选择最早且依赖满足的 TODO。
2. 将任务标为 DOING，完成实现、验证和证据记录后才标 DONE。
3. 一轮可完成多个紧密相关项，但不能为展示绕过后端验收，也不扩展无关数据源或科研训练。
4. 401 等外部阻塞只阻塞依赖它的真实调用；记录 BLOCKED 原因并推进独立离线任务。
5. 模型规模化评测先核对已授权范围；已有授权不重复询问，不读取/输出秘密值。
6. 每轮更新下面的执行记录、README/CHANGELOG 中受影响的契约，然后按仓库约定提交、推送。
7. 修改计划时写清原因、影响的验收标准和替代方案；不能把失败任务直接改名为完成。
8. 进度汇报必须包含完成 ID、验证结果、剩余阻塞和下一项；不能只报“代码已写”。

## 7. 执行记录与下一步

| 日期 | 阶段/任务 | 结果与证据 | 下一步 |
|---|---|---|---|
| 2026-09-13 | 基线 | `9205d98`：29 项离线测试、无 LLM 流水线、本地项目扫描通过；真实调用遇到 401 | A1，随后 A2/A3 |
| 2026-09-13 | 计划 | 本文件成为后续开发的优先任务清单；尚未将 A–F 标为完成 | 下一轮从 A1 开始 |
| 2026-09-13 | A1 | `utils.get_llm_config()` 明确进程环境/Claude Code/项目 `.env` 与 provider 预设边界；新增统一 LLM 错误分类、脱敏和请求封装；`tests.test_utils` 7 项 + Agent 401 回归，全量 37/37 通过；`utils.py` 冒烟与无 LLM dry-run 通过 | A2，随后 A3 |

当前行动：A2 固定公开/合成样例、任务 schema、模型/提示词/数据版本和计数方式；
真实调用仍需有效凭证，A1 不自动换 key 或服务，凭证阻塞只影响 A4。
