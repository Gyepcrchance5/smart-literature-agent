# smart-literature-agent 环境变量模板
# 复制为 .env 后填入你自己的值（.env 已被 .gitignore 排除，不会上传到 GitHub）

# --- LLM 配置 ---

# [必填] 你的 Anthropic API key（或任何兼容 Anthropic Messages API 的代理 key）
# 获取：https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=sk-ant-xxxxxxxxxxxxxxxxxxxxxxxxxxxx

# [必填] 模型 ID
# 直连 Anthropic 官方时用 Claude 的公开模型 ID，例如：
#   claude-haiku-4-5-20251001     （便宜快速，默认推荐）
#   claude-sonnet-4-5             （更高质量）
#   claude-opus-4-7               （最强）
# 通过第三方代理时用代理要求的模型标识（参考代理文档）
LLM_MODEL=claude-haiku-4-5-20251001

# [可选] Anthropic API base URL
# 直连官方不需要设置此项（会使用 SDK 默认的 https://api.anthropic.com）
# 通过第三方代理时填代理地址，例如 https://your-proxy.example.com/anthropic
# ANTHROPIC_BASE_URL=

# --- DeepXiv ---
# DeepXiv 的 token 由 `deepxiv config` 命令自动管理在 ~/.env，
# 通常不需要在这里手动配置。如需覆盖可设：
# DEEPXIV_TOKEN=

# --- 可选：本项目行为 ---
# PYTHONIOENCODING=utf-8
# PYTHONUTF8=1
