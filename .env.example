# smart-literature-agent 环境变量模板
# 复制为 .env 后填入你自己的值（.env 已被 .gitignore 排除，不会上传到 GitHub）

# --- LLM 配置 ---

# [可选] 使用 config/keywords.yaml 中的 provider 预设：deepseek / anthropic / minimax
# 选定 provider 后，项目不会把 ~/.claude/settings.json 中可能属于其他服务的 key
# 自动带到该 provider；请在本文件或进程环境中提供对应的凭证。
# LLM_PROVIDER=

# [必填] 你的 Anthropic API key（或任何兼容 Anthropic Messages API 的代理 key）
# 获取：https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=your_api_key_here

# [可选] 模型 ID — 设置后覆盖 provider 预设；未设置时使用预设或 Claude Code 配置：
#   Anthropic 官方  → claude-haiku-4-5-20251001 / claude-sonnet-4-5 / claude-opus-4-7
#   DeepSeek 代理   → deepseek-v4-pro / deepseek-v4-flash
#   MiniMax 代理    → MiniMax-M2.7
LLM_MODEL=your_model_here

# [可选] API base URL（直连 Anthropic 官方不需要填；显式设置会覆盖 provider 预设）：
#   DeepSeek  → https://api.deepseek.com/anthropic
#   MiniMax   → https://api.minimaxi.com/anthropic
ANTHROPIC_BASE_URL=

# --- DeepXiv ---
# DeepXiv 的 token 由 `deepxiv config` 命令自动管理在 ~/.env，
# 通常不需要在这里手动配置。如需覆盖可设：
# DEEPXIV_TOKEN=

# --- OpenAlex ---
# [可选] 设置邮箱后进入 OpenAlex 的 polite pool，享受更稳定的 rate limit
# 不设置也能用（匿名池 10 req/s），但建议长期使用时填一个
# 参考：https://docs.openalex.org/how-to-use-the-api/rate-limits-and-authentication#the-polite-pool
# OPENALEX_MAILTO=you@example.com

# --- 可选：本项目行为 ---
# PYTHONIOENCODING=utf-8
# PYTHONUTF8=1
